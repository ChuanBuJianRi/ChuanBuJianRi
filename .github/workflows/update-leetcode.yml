name: Update LeetCode Stats

on:
  schedule:
    - cron: '0 0 * * *'  
  workflow_dispatch:     

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Update README with LeetCode Stats
        run: |
          echo "✅ LeetCode Stats (UTC)" >> log.txt
